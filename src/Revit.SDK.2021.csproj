<Project Sdk="Microsoft.NET.Sdk" ToolsVersion="Current">
	<PropertyGroup>
		<RevitVersion>2021</RevitVersion>
		<Version>$(RevitVersion).0.0.7</Version>
		<FileVersion>$(RevitVersion).0.0.383</FileVersion>
		<AssemblyVersion>$(RevitVersion).0.0.383</AssemblyVersion>
		<TargetPath>net48</TargetPath>
		<IsPackable>true</IsPackable>
	</PropertyGroup>

	<Import Project="Revit.SDK.Nuget.csproj" />
</Project>